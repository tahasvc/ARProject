using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloAR : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("Hello AR Projects :)");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
